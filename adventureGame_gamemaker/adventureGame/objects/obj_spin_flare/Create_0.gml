image_xscale = 0.1;
image_yscale = 0.1;
image_alpha = .5;

// Slightly randomize variables so waves aren't identical
target_scale = 2.0 + random(1.0); 
growth_speed = 0.12; 
rotation_speed = 10 + irandom(5);
fade_power = 0.93;